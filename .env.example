# Public browser token. Restrict the production token by asset and URL in Cesium ion.
VITE_CESIUM_ION_TOKEN=

# Server-only OpenAI credential. Never expose this with a VITE_ prefix.
OPENAI_API_KEY=

# Optional model and server overrides.
OPENAI_MODEL=gpt-5.4-mini
PORT=8787
